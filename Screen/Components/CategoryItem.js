// CategoryItem.js
import React from 'react';
import { View, Text } from 'react-native';

const CategoryItem = ({ item }) => {
  return (
    <View>
      <Text>Hello</Text>
      {/* Add more UI elements to display other item details */}
    </View>
  );
};

export default CategoryItem;
